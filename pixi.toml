[workspace]
authors = ["ARC345 <arnavrastogi.543@gmail.com>"]
channels = ["conda-forge"]
name = "tailor"
platforms = ["linux-64", "win-64"]
version = "0.1.0"

[tasks]
install-frontend = "npm install"
dev = { cmd = "npm run tauri:dev", env = { PKG_CONFIG_PATH = "/usr/lib/pkgconfig", LIBRARY_PATH = "/usr/lib" } }
build = { cmd = "npm run tauri:build", env = { PKG_CONFIG_PATH = "/usr/lib/pkgconfig", LIBRARY_PATH = "/usr/lib" } }
sidecar = "python -m sidecar"
test = "pytest sidecar/tests"

[dependencies]
python = ">=3.14.2,<3.15"
nodejs = ">=25.2.1,<25.3"
langchain = ">=1.2.3,<2"
langchain-openai = ">=1.1.7,<2"
websockets = ">=15.0.1,<16"
loguru = ">=0.7.3,<0.8"
python-dotenv = ">=1.2.1,<2"
langgraph = ">=1.0.5,<2"
pytest = ">=9.0.2,<10"
pytest-asyncio = ">=1.3.0,<2"
pytest-cov = ">=7.0.0,<8"
pytest-mock = ">=3.15.1,<4"
rust = ">=1.92.0,<1.93"
make = ">=4.4.1,<5"
glib = ">=2.86.3,<3"
pkg-config = ">=0.29.2,<0.30"
openssl = ">=3.2.1,<4"
pillow = ">=12.1.0,<13"
pydantic = ">=2.12.5,<3"

[pypi-dependencies]
langgraph-cli = ">=0.4.11, <0.5"
